import type { SkillNode } from '../types';

// ═══════════════════════════════════════════════
// БЛОК: React — основы (вопросы #119-141)
// ═══════════════════════════════════════════════
export const REACT_BASICS: SkillNode = {
  id: 'react-basics',
  title: 'React: основы',
  category: 'React',
  description: 'Что такое React, JSX, компоненты, хуки, state, props, Virtual DOM, композиция и однонаправленный поток данных.',
  iconName: 'Zap',
  lessons: [
    // ─── Урок 1: Что такое React и JSX ───
    {
      id: 'react-intro-jsx',
      title: 'React и JSX',
      xpReward: 10,
      slides: [
        // ✅ Theory #119 — Что такое React? Перечислите его преимущества.
        {
          type: 'theory',
          title: 'Что такое React?',
          definition:
            'React — это JavaScript-библиотека (не фреймворк) для построения пользовательских интерфейсов, созданная Facebook. Ключевая идея: UI = f(state) — интерфейс является функцией от состояния.',
          keyTerms: [
            { term: 'Декларативный подход', definition: 'Описываем КАК должен выглядеть UI при данном состоянии, а не КАК менять DOM' },
            { term: 'Компонентная модель', definition: 'UI разбит на изолированные переиспользуемые блоки — компоненты' },
            { term: 'Virtual DOM', definition: 'Виртуальное дерево объектов, которое React сравнивает со старым и обновляет только изменённые части реального DOM' },
            { term: 'Однонаправленный поток данных', definition: 'Данные передаются строго от родителя к ребёнку через props' },
          ],
          pitfalls: [
            'React — библиотека, а не фреймворк: не навязывает роутинг, хранение данных и серверную часть.',
            'React отвечает только за View-слой — остальное добирается библиотеками (react-router, zustand и т.д.).',
            'Декларативный подход заменяет императивные querySelector/addEventListener на описание результата.',
          ],
          mnemonic: 'React = UI как функция состояния. Раньше: «нажали кнопку → поменяй DOM». React: «опиши, как выглядит UI при данном state — React обновит сам».',
        },
        {
          type: 'multiple-choice',
          // ✅ Theory #119
          question: 'Что такое React? Перечислите его преимущества.',
          options: [
            'React — это полноценный фреймворк с встроенным роутером, ORM и серверной частью, как Django',
            'React — это JS-библиотека для построения UI. Преимущества: декларативный подход (UI = f(state)), компонентная модель, Virtual DOM для эффективных обновлений, однонаправленный поток данных, огромная экосистема',
            'React — это шаблонизатор HTML, аналог Handlebars, который просто вставляет данные в разметку',
            'React — это CSS-фреймворк для стилизации компонентов, аналог Bootstrap',
          ],
          correctIndex: 1,
          explanation: 'React — JS-библиотека (не фреймворк!) для UI. Преимущества: (1) декларативность — описываем результат, не процесс, (2) компоненты — изолированные переиспользуемые блоки, (3) Virtual DOM — минимальные обновления реального DOM, (4) однонаправленный поток данных — предсказуемость, (5) огромное сообщество и экосистема библиотек.',
        },
        // ✅ Theory #120 — Что такое JSX и как он работает?
        {
          type: 'theory',
          title: 'Что такое JSX?',
          definition:
            'JSX (JavaScript XML) — синтаксическое расширение JavaScript, позволяющее описывать UI в виде, похожем на HTML. Это НЕ HTML и НЕ отдельный язык — это удобная запись, которая компилируется в вызовы React.createElement().',
          codeExample: `// JSX:
const element = <h1>Hello</h1>;

// Компилируется в:
const element = React.createElement("h1", null, "Hello");

// Под капотом JSX — это объект:
// { type: "h1", props: { children: "Hello" } }`,
          pitfalls: [
            'JSX не понимается браузером напрямую — его компилирует Babel/SWC в обычный JS.',
            'В JSX используются JS-выражения в фигурных скобках: {name}, {items.map(...)}, {condition && <El />}.',
            'JSX — это объекты. React работает не с DOM, а с деревом таких объектов (Virtual DOM).',
          ],
          keyTerms: [
            { term: 'JSX', definition: 'Синтаксическое расширение JS — визуально похоже на HTML, но компилируется в React.createElement()' },
            { term: 'React.createElement()', definition: 'Функция, создающая объект-описание элемента: { type, props, children }' },
            { term: 'Babel/SWC', definition: 'Компиляторы, которые превращают JSX в обычный JavaScript' },
          ],
        },
        {
          type: 'multiple-choice',
          // ✅ Theory #120
          question: 'Что такое JSX и как он работает?',
          options: [
            'JSX — это отдельный язык разметки, который браузер понимает нативно, как HTML',
            'JSX — это синтаксическое расширение JavaScript, похожее на HTML. Компилируется (Babel/SWC) в вызовы React.createElement(). Браузер JSX напрямую не понимает',
            'JSX — это строковый шаблон (template literal), который вставляется через innerHTML',
            'JSX — это CSS-in-JS решение для стилизации React-компонентов',
          ],
          correctIndex: 1,
          explanation: 'JSX = синтаксический сахар над React.createElement(). <h1>Hello</h1> → React.createElement("h1", null, "Hello") → объект { type: "h1", props: { children: "Hello" } }. Компилируется сборщиком (Babel/SWC) ДО запуска в браузере. JSX позволяет описывать UI декларативно, вставляя JS-выражения через {  }.',
        },
        // ✅ Theory #130 — Разница между JSX и HTML?
        {
          type: 'multiple-choice',
          // ✅ Theory #130
          question: 'Разница между JSX и HTML?',
          options: [
            'Нет разницы — JSX это просто HTML внутри JavaScript-файла',
            'JSX компилируется в JS (React.createElement), использует className вместо class, htmlFor вместо for, camelCase атрибуты (onClick), обязательно закрывает теги (<img />, <br />), позволяет вставлять JS-выражения через { }',
            'JSX — это HTML, который работает только в Node.js, а не в браузере',
            'Единственная разница — JSX поддерживает компоненты с большой буквы',
          ],
          correctIndex: 1,
          explanation: 'Ключевые отличия JSX от HTML: (1) className вместо class (class — зарезервированное слово в JS), (2) htmlFor вместо for, (3) camelCase для атрибутов: onClick, onChange, tabIndex, (4) все теги должны быть закрыты: <img />, <br />, <input />, (5) JS-выражения через { }: {name}, {items.map(...)}, (6) style принимает объект: style={{ color: "red" }}, (7) компоненты — с большой буквы: <Button />, HTML-теги — с маленькой: <button>.',
        },
      ],
    },
    // ─── Урок 2: Хуки (hooks) ───
    {
      id: 'react-hooks-basics',
      title: 'React Hooks',
      xpReward: 10,
      slides: [
        // ✅ Theory #121 — Что такое React хуки (Hooks)?
        {
          type: 'theory',
          title: 'Что такое React хуки (Hooks)?',
          definition:
            'Хук — это специальная функция React, которая позволяет «подключиться» к возможностям React (состояние, жизненный цикл, контекст) внутри функционального компонента. Хук даёт функции «память» и доступ к жизненному циклу.',
          keyTerms: [
            { term: 'useState', definition: 'Хук для добавления состояния (внутренней памяти) в функциональный компонент' },
            { term: 'useEffect', definition: 'Хук для выполнения побочных эффектов после рендера (запросы, подписки, DOM)' },
            { term: 'Кастомный хук', definition: 'Обычная функция, начинающаяся с use, которая использует другие хуки и инкапсулирует логику' },
          ],
          pitfalls: [
            'Имя хука всегда начинается с use — это соглашение React (useState, useEffect, useMemo и т.д.).',
            'До хуков (React 16.8) для state и жизненного цикла нужны были классовые компоненты.',
            'Хуки полностью заменили классовые компоненты — сейчас всё пишется на функциональных.',
          ],
        },
        {
          type: 'multiple-choice',
          // ✅ Theory #121
          question: 'Что такое React хуки (Hooks)?',
          options: [
            'Хуки — это CSS-классы для стилизации React-компонентов',
            'Хуки — это специальные функции React (начинаются с use), которые дают функциональным компонентам доступ к состоянию, жизненному циклу и другим возможностям React. Появились в React 16.8, заменив необходимость в классовых компонентах',
            'Хуки — это события браузера (onclick, onchange), обёрнутые в React',
            'Хуки — это декораторы для классовых компонентов React',
          ],
          correctIndex: 1,
          explanation: 'Хуки — функции с префиксом use, которые подключают функциональные компоненты к возможностям React: useState (состояние), useEffect (побочные эффекты), useContext (контекст), useMemo/useCallback (оптимизация). До React 16.8 для этого нужны были классы с this.state и componentDidMount/componentDidUpdate.',
        },
        // ✅ Theory #124 — Какие преимущества дают хуки в React?
        {
          type: 'multiple-choice',
          // ✅ Theory #124
          question: 'Какие преимущества дают хуки (hooks) в React?',
          options: [
            'Хуки ускоряют рендеринг в 10 раз по сравнению с классами',
            'Хуки позволяют: переиспользовать логику между компонентами (кастомные хуки), писать компактнее без классов и this, разделять логику по смыслу (несколько useEffect), проще тестировать и читать код',
            'Хуки нужны только для работы с API — для UI они не используются',
            'Хуки позволяют писать компоненты без JSX',
          ],
          correctIndex: 1,
          explanation: 'Преимущества хуков: (1) переиспользование логики — кастомные хуки (useFetch, useCounter) извлекают повторяющуюся логику без HOC/render props, (2) компактность — нет class, constructor, this.bind, (3) разделение по смыслу — вместо одного componentDidMount с кучей логики, несколько useEffect для разных задач, (4) проще тестировать — обычные функции.',
        },
        // ✅ Theory #125 — Каковы правила использования хуков?
        {
          type: 'theory',
          title: 'Правила использования хуков',
          definition:
            'React накладывает строгие ограничения на вызов хуков. Нарушение приводит к ошибкам, потому что React отслеживает хуки по порядку вызова.',
          pitfalls: [
            'Правило 1: Хуки вызываются ТОЛЬКО внутри функционального компонента или другого хука. Нельзя вызывать в обычной функции, классе или вне React.',
            'Правило 2: Хуки вызываются ТОЛЬКО на верхнем уровне — нельзя в if, for, вложенных функциях. React запоминает хуки по порядку вызова.',
            'Если порядок хуков изменится (например, хук в условии не вызвался), React потеряет связь состояния с компонентом.',
          ],
          codeExample: `// ❌ Неправильно — хук в условии:
if (isLoggedIn) {
  const [name, setName] = useState('');
}

// ✅ Правильно — хук на верхнем уровне:
const [name, setName] = useState('');
// условие внутри хука или после:
useEffect(() => {
  if (isLoggedIn) { fetchUser(); }
}, [isLoggedIn]);`,
        },
        {
          type: 'multiple-choice',
          // ✅ Theory #125
          question: 'Каковы правила использования хуков в React?',
          options: [
            'Хуки можно вызывать где угодно: в циклах, условиях, вложенных функциях',
            'Хуки можно вызывать только в классовых компонентах',
            'Два правила: (1) вызывать только внутри функциональных компонентов или других хуков, (2) вызывать только на верхнем уровне — не в if/for/вложенных функциях. React отслеживает хуки по порядку вызова',
            'Хуки обязательно вызывать в useEffect, чтобы они работали после рендера',
          ],
          correctIndex: 2,
          explanation: 'Два строгих правила: (1) Хуки — только в функциональных компонентах или кастомных хуках (не в обычных функциях или классах), (2) Только на верхнем уровне — не в if, for, while, вложенных функциях. Причина: React запоминает хуки по порядку вызова (1-й вызов = 1-й state, 2-й = 2-й). Если порядок нарушится — состояние перепутается.',
        },
        // ✅ Theory #133 — Правила (ограничения) использования хуков?
        {
          type: 'true-false',
          // ✅ Theory #133
          question: 'Хуки в React можно вызывать внутри условий (if) и циклов (for), главное — внутри функционального компонента.',
          correctValue: false,
          explanation: 'Нельзя! Хуки обязательно вызываются на верхнем уровне компонента — не в if, for, while или вложенных функциях. React идентифицирует хуки по порядку вызова. Если хук пропустится из-за условия, порядок сдвинется и состояние перепутается.',
        },
      ],
    },
    // ─── Урок 3: State и Props ───
    {
      id: 'react-state-props',
      title: 'State и Props',
      xpReward: 10,
      slides: [
        // ✅ Theory #122 — В чём разница между state и props?
        {
          type: 'theory',
          title: 'State vs Props',
          definition:
            'Props (properties) — входные параметры компонента, приходят от родителя, только для чтения. State — внутренние данные (память) компонента, могут изменяться, принадлежат самому компоненту.',
          comparison: {
            title: 'Props vs State',
            headers: ['Характеристика', 'Props', 'State'],
            rows: [
              ['Источник', 'Приходят извне (от родителя)', 'Хранятся внутри компонента'],
              ['Изменяемость', 'Только для чтения (read-only)', 'Можно изменять через setState'],
              ['Управление', 'Управляются родителем', 'Управляются самим компонентом'],
              ['Принадлежность', 'Не принадлежат компоненту', 'Принадлежат компоненту'],
              ['Пример', '<Button text="OK" />', 'const [count, setCount] = useState(0)'],
            ],
          },
          pitfalls: [
            'props.name = "Боб" — НЕЛЬЗЯ! Props неизменяемы (immutable).',
            'Если props изменятся у родителя → ребёнок перерисуется с новыми props.',
            'Если state изменится → компонент перерисуется, UI обновится.',
          ],
        },
        {
          type: 'multiple-choice',
          // ✅ Theory #122
          question: 'В чём разница между state и props в React?',
          options: [
            'Нет разницы — это синонимы для данных компонента',
            'Props — внешние данные от родителя (read-only), state — внутренние данные компонента (изменяемые через setState). Props управляются родителем, state — самим компонентом',
            'State приходит от родителя, а props создаются внутри компонента',
            'Props используются только для строк, state — только для чисел',
          ],
          correctIndex: 1,
          explanation: 'Props — входные параметры, переданные родителем: <Child name="Ann" />. Только для чтения, компонент не может их изменить. State — внутренняя память: const [count, setCount] = useState(0). Изменяется через setter, вызывает перерисовку. Аналогия: props = аргументы функции (приходят извне), state = локальные переменные (живут внутри).',
        },
        // ✅ Theory #131 — Разница между состоянием (state) и пропсами (props)?
        {
          type: 'true-false',
          // ✅ Theory #131
          question: 'Props в React можно изменять внутри дочернего компонента — например, написать props.name = "Боб".',
          correctValue: false,
          explanation: 'Нельзя! Props — read-only. React строится на однонаправленном потоке данных: родитель → ребёнок. Ребёнок не может изменить props напрямую. Если нужно «отправить данные наверх», родитель передаёт функцию-колбэк через props, и ребёнок её вызывает.',
        },
        // ✅ Theory #123 — Для чего в React нужен проп `key`?
        {
          type: 'theory',
          title: 'Проп key в React',
          definition:
            'key — специальный атрибут React для идентификации элементов в списках. React использует key, чтобы понять: это тот же элемент (update) или новый (unmount старого + mount нового).',
          codeExample: `// ✅ Правильно — уникальный стабильный key:
{items.map(item => (
  <Item key={item.id} />
))}

// ❌ Плохо — индекс как key:
{items.map((item, index) => (
  <Item key={index} />  // при перестановке/удалении — баги
))}`,
          pitfalls: [
            'key должен быть уникальным среди соседних элементов и стабильным (не меняться между рендерами).',
            'key НЕ передаётся в props — он недоступен внутри компонента. Нужен id? Передавай отдельным пропом.',
            'Если key изменился — React удалит старый компонент и создаст новый (полный unmount + mount).',
          ],
        },
        {
          type: 'multiple-choice',
          // ✅ Theory #123
          question: 'Для чего в React нужен проп `key`?',
          options: [
            'key — это CSS-идентификатор для стилизации элементов списка',
            'key нужен для SEO — поисковики используют его для индексации',
            'key — уникальный идентификатор элемента в списке, по которому React определяет: обновить существующий элемент (key не изменился) или удалить старый и создать новый (key изменился)',
            'key нужен только для input-элементов, чтобы React привязывал значение',
          ],
          correctIndex: 2,
          explanation: 'key помогает React различать элементы при обновлении списков. Без key React сравнивает элементы по порядку — при вставке/удалении/перестановке это приводит к багам. key должен быть уникальным и стабильным (идеально — id из данных). key не передаётся в props компонента — для доступа к id внутри нужен отдельный проп.',
        },
      ],
    },
    // ─── Урок 4: useEffect и жизненный цикл ───
    {
      id: 'react-useeffect',
      title: 'useEffect и жизненный цикл',
      xpReward: 10,
      slides: [
        // ✅ Theory #126 — На что влияет массив зависимостей useEffect?
        {
          type: 'theory',
          title: 'Массив зависимостей useEffect',
          definition:
            'useEffect принимает функцию (эффект) и опциональный массив зависимостей. Массив зависимостей определяет, КОГДА эффект будет запускаться.',
          comparison: {
            title: 'Три режима useEffect',
            headers: ['Вариант', 'Когда запускается', 'Аналог в классах'],
            rows: [
              ['useEffect(fn)', 'После каждого рендера (mount + update)', 'componentDidMount + componentDidUpdate'],
              ['useEffect(fn, [])', 'Только при mount (один раз)', 'componentDidMount'],
              ['useEffect(fn, [dep])', 'При mount + при изменении dep', 'componentDidUpdate с проверкой'],
            ],
          },
          pitfalls: [
            'Без массива зависимостей эффект запускается после КАЖДОГО рендера — это почти всегда лишнее.',
            'React сравнивает зависимости поверхностно (shallow) — объекты/массивы сравниваются по ссылке.',
            'В массив зависимостей нужно добавлять ВСЁ, что используется внутри эффекта из внешней области видимости.',
            'useEffect может вернуть cleanup-функцию — она вызывается при unmount и перед повторным запуском эффекта.',
          ],
          codeExample: `// Загрузка данных при mount:
useEffect(() => {
  fetch('/api/users').then(r => r.json()).then(setUsers);
}, []); // пустой массив → один раз

// Реакция на изменение userId:
useEffect(() => {
  fetch(\`/api/users/\${userId}\`).then(r => r.json()).then(setUser);
}, [userId]); // запускается при смене userId

// Cleanup (очистка):
useEffect(() => {
  const id = setInterval(() => console.log('tick'), 1000);
  return () => clearInterval(id); // cleanup при unmount
}, []);`,
        },
        {
          type: 'multiple-choice',
          // ✅ Theory #126
          question: 'На что влияет массив зависимостей у `useEffect`?',
          options: [
            'Массив зависимостей определяет, какие переменные React должен мемоизировать',
            'Массив зависимостей управляет тем, КОГДА запускается эффект: без массива — после каждого рендера, [] — только при mount, [dep] — при mount и при изменении dep',
            'Массив зависимостей — это список пропсов, которые компонент принимает',
            'Массив зависимостей определяет порядок рендеринга дочерних компонентов',
          ],
          correctIndex: 1,
          explanation: 'Массив зависимостей — второй аргумент useEffect, контролирует запуск: (1) без массива — после каждого рендера (редко нужно), (2) [] — только при mount (аналог componentDidMount), (3) [value, userId] — при mount и при изменении value или userId. React сравнивает зависимости поверхностно (===). Cleanup-функция вызывается перед повторным запуском и при unmount.',
        },
        // ✅ Theory #132 — Разница между useEffect() и componentDidMount()?
        {
          type: 'multiple-choice',
          // ✅ Theory #132
          question: 'Разница между `useEffect()` и `componentDidMount()`?',
          options: [
            'Нет разницы — useEffect с пустым массивом полностью идентичен componentDidMount',
            'useEffect(() => {}, []) — хук для функциональных компонентов, запускается после рендера асинхронно (не блокирует paint). componentDidMount — метод классовых компонентов. useEffect может возвращать cleanup-функцию и зависеть от переменных; componentDidMount — только при первом монтировании',
            'componentDidMount — для функциональных компонентов, useEffect — для классовых',
            'useEffect запускается ДО рендера, а componentDidMount — ПОСЛЕ',
          ],
          correctIndex: 1,
          explanation: 'useEffect(() => fn, []) ≈ componentDidMount, но не идентичен: (1) useEffect — для функциональных компонентов, componentDidMount — для классовых, (2) useEffect запускается асинхронно после paint (не блокирует отрисовку), componentDidMount — синхронно после mount, (3) useEffect поддерживает cleanup (return () => {}), зависимости и может реагировать на обновления, (4) componentDidMount вызывается ровно один раз.',
        },
        // ✅ Theory #127 — Что означает повторный рендер (re-rendering)?
        {
          type: 'multiple-choice',
          // ✅ Theory #127
          question: 'Что означает повторный рендер (re-rendering) в React?',
          options: [
            'Re-render = браузер перерисовывает всю страницу с нуля, включая все DOM-элементы',
            'Re-render = React заново вызывает функцию компонента, строит новое виртуальное дерево, сравнивает с предыдущим и обновляет только изменившиеся части реального DOM',
            'Re-render = компонент удаляется и создаётся заново (unmount + mount)',
            'Re-render = React обновляет только CSS-стили, не трогая HTML',
          ],
          correctIndex: 1,
          explanation: 'Re-render ≠ перерисовка всей страницы. React заново вызывает функцию-компонент → получает новое виртуальное дерево → сравнивает со старым (reconciliation) → обновляет только изменённые части DOM. Компонент «рендерится», но DOM может вообще не измениться, если результат тот же. Re-render вызывается при: изменении state, получении новых props, перерисовке родителя.',
        },
        // ✅ Theory #141 — Что означает повторный рендер (re-rendering)? (дубль)
        {
          type: 'true-false',
          // ✅ Theory #141
          question: 'В React повторный рендер (re-render) компонента всегда означает обновление реального DOM.',
          correctValue: false,
          explanation: 'Рендер ≠ обновление DOM. Render-фаза: React вызывает функцию компонента и строит новое виртуальное дерево. Commit-фаза: React сравнивает деревья и обновляет DOM ТОЛЬКО если есть различия. Компонент может «рендериться» (вызваться как функция), но DOM останется без изменений, если результат совпадает.',
        },
      ],
    },
    // ─── Урок 5: Композиция, Virtual DOM, поток данных ───
    {
      id: 'react-composition-vdom',
      title: 'Композиция, Virtual DOM, поток данных',
      xpReward: 10,
      slides: [
        // ✅ Theory #128 — Паттерн композиции в React
        {
          type: 'multiple-choice',
          // ✅ Theory #128
          question: 'Объясните паттерн композиции (composition pattern) в React.',
          options: [
            'Композиция — это наследование от базового React-компонента через extends',
            'Композиция — это построение сложного UI из простых компонентов путём вложения друг в друга. Компонент использует children или пропсы для отображения переданного контента, не зная о его внутренностях. Пример: <Card><Title /><Button /></Card>',
            'Композиция — это объединение всех компонентов в один файл для удобства',
            'Композиция — это паттерн CSS для компоновки блоков через Flexbox',
          ],
          correctIndex: 1,
          explanation: 'Композиция — основной способ построения UI в React (вместо наследования). Компоненты собираются как конструктор: <Layout><Header /><Main>{children}</Main><Footer /></Layout>. Ключевое: children — специальный prop, позволяющий передавать произвольный JSX внутрь компонента. Компонент-обёртка не знает, что внутри него — он просто рендерит {children}.',
        },
        // ✅ Theory #129 — Что такое Virtual DOM?
        {
          type: 'theory',
          title: 'Virtual DOM в React',
          definition:
            'Virtual DOM — лёгкое JavaScript-представление (дерево объектов) реального DOM. React работает не с реальным DOM, а с виртуальным: при изменении state строит новое виртуальное дерево, сравнивает с предыдущим (reconciliation/diffing) и применяет минимальные изменения к реальному DOM.',
          diagram: {
            type: 'flow',
            title: 'Как работает Virtual DOM',
            items: [
              'Изменился state/props',
              'React вызывает компонент заново → новое виртуальное дерево',
              'Diffing: сравнение нового дерева со старым',
              'Reconciliation: определение минимальных изменений',
              'Commit: применение изменений к реальному DOM',
            ],
          },
          pitfalls: [
            'Virtual DOM — не быстрее нативных DOM-операций напрямую. Он быстрее «наивного» подхода, когда перерисовывают всё.',
            'Реальный DOM дорогой: каждый узел содержит сотни свойств (style, events, layout). Объекты Virtual DOM — лёгкие: {type, props, children}.',
            'key помогает diffing-алгоритму правильно идентифицировать элементы в списках.',
          ],
        },
        {
          type: 'multiple-choice',
          // ✅ Theory #129
          question: 'Что такое виртуальный DOM (Virtual DOM) в React?',
          options: [
            'Virtual DOM — это реальный DOM, но в другом окне браузера (iframe)',
            'Virtual DOM — лёгкое JS-представление реального DOM (дерево объектов). React строит новое виртуальное дерево при изменении state, сравнивает с предыдущим (diffing) и обновляет только изменившиеся части реального DOM',
            'Virtual DOM — это DOM, который существует только на сервере и отправляется клиенту',
            'Virtual DOM — это кэш HTML-страницы в localStorage',
          ],
          correctIndex: 1,
          explanation: 'Virtual DOM = дерево JS-объектов {type: "div", props: {children: [...]}}. При setState: (1) React вызывает компонент → новое виртуальное дерево, (2) diffing — сравнение нового и старого деревьев, (3) commit — только изменённые узлы обновляются в реальном DOM. Это быстрее «наивного» подхода с innerHTML = ... или перерисовкой всего. Ключевая роль — алгоритм reconciliation с O(n) сложностью.',
        },
        // ✅ Theory #139 — Как в React работает VDOM? Преимущества и недостатки?
        {
          type: 'multiple-choice',
          // ✅ Theory #139
          question: 'Как в React работает виртуальный DOM? Какие у него преимущества и недостатки?',
          options: [
            'VDOM ускоряет всё в 100 раз и не имеет недостатков',
            'VDOM — дерево JS-объектов. Преимущества: минимальные обновления DOM, декларативный код, кроссплатформенность (React Native). Недостатки: дополнительное потребление памяти на хранение двух деревьев, overhead на diffing при частых мелких обновлениях, не всегда быстрее точечных нативных DOM-операций',
            'VDOM хранится на сервере и обновляет клиент через WebSocket',
            'VDOM — это Shadow DOM из Web Components',
          ],
          correctIndex: 1,
          explanation: 'Преимущества VDOM: (1) минимальные обновления реального DOM — меняем только то, что изменилось, (2) декларативный код — описываем результат, не процесс, (3) кроссплатформенность — VDOM можно рендерить не только в DOM (React Native → мобильные приложения). Недостатки: (1) занимает память на два дерева, (2) diffing — вычислительный overhead, (3) для простых точечных обновлений нативный DOM может быть быстрее. VDOM ≠ Shadow DOM (это разные концепции).',
        },
        // ✅ Theory #138 — Однонаправленный поток данных
        {
          type: 'multiple-choice',
          // ✅ Theory #138
          question: 'Объясните однонаправленный поток данных в React и его преимущества.',
          options: [
            'Данные в React передаются в обе стороны: ребёнок может изменять props родителя',
            'Данные текут строго от родителя к ребёнку через props. Ребёнок не может изменить данные родителя напрямую. Для «обратной связи» родитель передаёт функцию-колбэк. Преимущества: предсказуемость, контролируемость, простота отладки',
            'Однонаправленный поток — данные идут от сервера к клиенту, но не обратно',
            'Однонаправленный поток — данные передаются только через глобальные переменные',
          ],
          correctIndex: 1,
          explanation: 'Однонаправленный поток данных (one-way data flow): Parent → Child через props. Ребёнок НЕ МОЖЕТ изменить props или state родителя напрямую. Для обратной связи: Parent передаёт функцию (<Child onChange={handleChange} />), ребёнок вызывает её. Преимущества: (1) предсказуемость — всегда ясно, откуда пришли данные, (2) лёгкая отладка — поток данных легко отследить, (3) масштабируемость — изменения локализованы.',
        },
        // ✅ Theory #134 — Lifting State Up
        {
          type: 'multiple-choice',
          // ✅ Theory #134
          question: 'Что такое поднятие состояния вверх (Lifting State Up)?',
          options: [
            'Перенос state из компонента в глобальную переменную window',
            'Перемещение общего state в ближайшего общего родителя двух компонентов, которым эти данные нужны. Родитель хранит state и передаёт данные + setter через props. Компоненты-дети становятся «управляемыми»',
            'Копирование state из ребёнка в родителя через props',
            'Использование useRef вместо useState для хранения данных',
          ],
          correctIndex: 1,
          explanation: 'Lifting State Up — паттерн React: если двум компонентам нужны одни и те же данные, state поднимается в их ближайшего общего родителя. Родитель владеет state и передаёт вниз через props (данные + функцию изменения). Пример: два инпута температуры (Цельсий/Фаренгейт) — state хранится в родителе, оба инпута получают значение и onChange через props.',
        },
      ],
    },
    // ─── Урок 6: useState — нюансы ───
    {
      id: 'react-usestate-details',
      title: 'useState: нюансы и подводные камни',
      xpReward: 10,
      slides: [
        // ✅ Theory #135 — Индексы массива как key
        {
          type: 'multiple-choice',
          // ✅ Theory #135
          question: 'К каким последствиям приводит использование индексов массива в качестве значений `key` в React?',
          options: [
            'Никаких — индекс массива это лучший вариант для key',
            'При добавлении/удалении/перестановке элементов индексы сдвигаются → React считает, что элемент изменился (хотя данные те же) или не изменился (хотя данные другие). Последствия: потеря состояния инпутов, неправильные анимации, лишние ререндеры, некорректное отображение',
            'Индексы как key замедляют рендеринг в 10 раз',
            'Индексы как key работают только для списков меньше 100 элементов',
          ],
          correctIndex: 1,
          explanation: 'При index как key: удалили элемент 0 → все индексы сдвинулись → React думает, что все элементы «обновились» (хотя они просто сдвинулись). Проблемы: (1) input сохранит state от ДРУГОГО элемента (перепутает), (2) анимации сломаются, (3) лишние ререндеры. Используй стабильный уникальный id из данных: key={item.id}. index допустим ТОЛЬКО для статических списков без перестановок/удалений.',
        },
        // ✅ Theory #136 — Функция-колбэк у setState
        {
          type: 'multiple-choice',
          // ✅ Theory #136
          question: 'Зачем у setter\'а состояния (`setState` / `useState`) есть форма с функцией-колбэком и когда её использовать?',
          options: [
            'Колбэк-форма нужна только для асинхронных операций с сервером',
            'setValue(prev => prev + 1) гарантирует работу с актуальным значением state, а не устаревшим из замыкания. Использовать когда: новое значение зависит от предыдущего, несколько обновлений подряд, обновление в асинхронном коде (после await)',
            'Колбэк-форма — это устаревший синтаксис, setValue(value + 1) всегда правильнее',
            'Колбэк-форма нужна только для объектов, для примитивов — не нужна',
          ],
          correctIndex: 1,
          explanation: 'setValue(value + 1) использует value из замыкания — может быть устаревшим. setValue(prev => prev + 1) получает актуальное значение от React. Пример проблемы: setValue(value+1); setValue(value+1) → оба используют одно и то же value → результат +1, а не +2. С колбэком: setValue(p=>p+1); setValue(p=>p+1) → гарантированно +2. Используй колбэк, когда новый state зависит от предыдущего.',
        },
        // ✅ Theory #137 — Почему не мутировать state
        {
          type: 'multiple-choice',
          // ✅ Theory #137
          question: 'Почему в React не рекомендуют мутировать состояние?',
          options: [
            'Мутация state — это просто стилистическое правило, на работу не влияет',
            'React сравнивает state по ссылке. Мутация объекта (obj.age = 21; setObj(obj)) не создаёт новую ссылку → React не видит изменения и не перерисовывает компонент. Правильно: setObj({...obj, age: 21}) — новый объект = новая ссылка',
            'Мутация запрещена только для массивов, объекты мутировать можно',
            'Мутация state вызывает утечки памяти в браузере',
          ],
          correctIndex: 1,
          explanation: 'React определяет необходимость ререндера сравнением ссылок (===). Мутация: user.age = 21; setUser(user) — ссылка та же (тот же объект в памяти) → React считает «ничего не изменилось» → НЕ перерисовывает. Правильно: setUser({...user, age: 21}) или setUser(prev => ({...prev, age: prev.age + 1})) — создаём НОВЫЙ объект с новой ссылкой. Для массивов: [...items, newItem] вместо items.push(newItem).',
        },
        // ✅ Theory #140 — Что происходит при вызове setter из useState
        {
          type: 'multiple-choice',
          // ✅ Theory #140
          question: 'Объясните, что происходит при вызове функции-сеттера из `useState` в React.',
          options: [
            'setValue(10) мгновенно изменяет переменную value — можно сразу console.log(value) и увидеть 10',
            'setValue(10): React запоминает новое значение, планирует повторный рендер, вызывает компонент заново, useState возвращает новое значение. Обновление асинхронное — value в текущем замыкании остаётся старым',
            'setValue(10) отправляет новое значение на сервер и ждёт ответа',
            'setValue(10) напрямую изменяет DOM-элемент, без пересоздания компонента',
          ],
          correctIndex: 1,
          explanation: 'При вызове setValue(10): (1) React запоминает новое значение, (2) планирует повторный рендер (не мгновенно!), (3) React может батчить несколько обновлений в один ренд, (4) вызывает функцию-компонент заново, (5) useState(initialValue) возвращает уже НОВОЕ значение (10). Важно: value в текущем замыкании остаётся СТАРЫМ. Новое значение доступно только в следующем рендере.',
        },
      ],
    },
  ],
};
