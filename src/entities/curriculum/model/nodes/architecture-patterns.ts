import type { SkillNode } from '../types';

// ═══════════════════════════════════════════════
// БЛОК: Архитектура и компоненты (Week 07)
// Theory вопросы: #83–101
// ═══════════════════════════════════════════════
export const ARCHITECTURE_PATTERNS: SkillNode = {
  id: 'architecture-patterns',
  week: 7,
  title: 'Архитектура и паттерны',
  description: 'SRP, MVC, MVVM, Observer, Strategy, DI, EventEmitter, FSD-архитектура, компонентный подход',
  lessons: [],
  topics: [
    {
      id: 'arch-core',
      title: 'Архитектура фронтенда',
      slides: [
        {
          type: 'theory',
          title: 'Архитектура, паттерны и FSD',
          content: `**Архитектура** — способ организации кода: из каких частей состоит приложение, кто за что отвечает, как части взаимодействуют.

**Принципы:**
- **SRP** (Single Responsibility): одна причина для изменения у каждой части
- **SoC** (Separation of Concerns): каждая часть отвечает за одну область
- Высокая **связность (cohesion)** внутри, низкая **связанность (coupling)** между модулями

**Архитектурные паттерны:**
- **MVC** (Model–View–Controller): Model — данные, View — отображение, Controller — управление. View не содержит бизнес-логики.
- **MVVM**: ViewModel биндится с View напрямую → реактивность. React/Vue основаны на MVVM.

**Паттерны проектирования:**
- **Strategy**: инкапсулирует варианты поведения в отдельные сущности, выбираемые в рантайме
- **Dependency Injection**: компонент получает зависимости извне, а не создаёт сам
- **Observer/EventEmitter**: подписчики реагируют на события без прямых вызовов
- **Store + Observer**: единое хранилище состояния, компоненты подписываются на изменения

**FSD (Feature-Sliced Design):** слои сверху вниз: \`app → pages → widgets → features → entities → shared\`. Зависимости только вниз.`,
        },
        // ✅ Theory #83 — точная формулировка
        {
          type: 'multiple-choice',
          question: 'Что такое принцип единственной ответственности (Single Responsibility Principle, SRP)?',
          options: [
            'SRP — это когда в файле только одна функция',
            'У каждой части системы (функции, класса, компонента, модуля) должна быть одна причина для изменения. Если компонент меняется из-за изменений API, UI и логики — это нарушение SRP',
            'SRP означает что класс не должен иметь больше одного метода',
            'SRP — это принцип из React, не применимый к чистому JS',
          ],
          correctIndex: 1,
          explanation:
            'SRP (Single Responsibility Principle): у каждой части — одна причина для изменения. Пример нарушения: компонент, который получает данные из API, фильтрует их и рендерит — три причины изменения. Правильно: три отдельных части. Применяется ко всем уровням: функции (делает одно), классы (отвечает за одну сущность), компоненты (одна задача в UI), модули (одна предметная область). Практическое следствие: если сложно описать компонент одной фразой — он делает слишком много.',
        },
        // ✅ Theory #84 — точная формулировка
        {
          type: 'multiple-choice',
          question: 'Что такое паттерн Observer (наблюдатель)?',
          options: [
            'Observer — это когда компонент напрямую вызывает методы другого компонента',
            'Паттерн, при котором объект (subject) хранит список подписчиков и уведомляет их при изменении своего состояния. Подписчики реагируют автоматически без прямых вызовов между ними',
            'Observer — это синхронный цикл опроса состояния',
            'Observer — это то же самое что Promise',
          ],
          correctIndex: 1,
          explanation:
            'Observer (наблюдатель): Subject хранит список observers (подписчиков). При изменении вызывает observer.update() у всех. Подписчики не знают друг о друге, Subject не знает, что именно делают подписчики. Пример: UserModel.setUser(data) → все подписанные компоненты (Header, Profile, Analytics) автоматически получают обновление. Применения: EventEmitter, Redux store, реактивные системы (Vue, MobX). Отличие от EventEmitter: Observer — паттерн для одного объекта-источника, EventEmitter — общая шина событий.',
        },
        // ✅ Theory #85 — точная формулировка
        {
          type: 'multiple-choice',
          question: 'Что такое архитектурный паттерн MVC (Model-View-Controller)?',
          options: [
            'MVC — это библиотека для управления состоянием в React',
            'MVC разделяет приложение на три части: Model (данные и бизнес-логика), View (отображение), Controller (принимает действия пользователя, обновляет Model и View). View не содержит бизнес-логики, Model не знает о View',
            'MVC означает что код разделён на три файла',
            'MVC и MVVM — одно и то же',
          ],
          correctIndex: 1,
          explanation:
            'MVC: Model — данные, правила работы с ними, состояние приложения. View — отображение данных пользователю (HTML/шаблон). Controller — посредник: принимает действия пользователя, обновляет Model, говорит View перерисоваться. Ключевая мысль: View не содержит бизнес-логики, Model не знает о представлении. Пример: нажатие кнопки → Controller обрабатывает → Model обновляется → View перерисовывается. Используется: бэкенд (Django, Rails), старые SPA, jQuery-проекты. Отличие от MVVM: в MVC Controller явно управляет View; в MVVM View сам обновляется при изменении ViewModel.',
        },
        // ✅ Theory #94 — точная формулировка
        {
          type: 'multiple-choice',
          question: 'В чём разница между MVC и MVVM? Когда какой подход уместен?',
          options: [
            'MVC и MVVM решают разные задачи и не связаны',
            'MVC: Controller явно обновляет View. MVVM: ViewModel биндится с View напрямую — интерфейс сам обновляется при изменении данных (реактивность). MVVM — основа React/Vue/Angular',
            'MVVM используется только на мобильных платформах',
            'В MVC нет Model, в MVVM есть',
          ],
          correctIndex: 1,
          explanation:
            'MVC: Controller обрабатывает действия пользователя и явно говорит View что обновить. View может содержать код. Используется: бэкенд (Django, Express), старые SPA. MVVM: ViewModel хранит состояние и биндится с View через data binding. View максимально "тупой" — просто отображает данные. При изменении ViewModel → View обновляется сам (реактивность). Используется: React (useState/props), Vue (reactive), Angular, SwiftUI. Ключевое: в MVC обновление вручную; в MVVM автоматически через реактивность.',
        },
        // ✅ Theory #95 — точная формулировка
        {
          type: 'multiple-choice',
          question: 'Что такое паттерн Strategy и когда его применять?',
          options: [
            'Strategy — это паттерн для создания объектов',
            'Strategy инкапсулирует разные варианты поведения в отдельные сущности и позволяет выбирать нужное поведение в рантайме. Применять когда есть множество вариантов алгоритма с одинаковым интерфейсом и их набор может расширяться',
            'Strategy — это когда весь код в одной функции с if/else',
            'Strategy применяется только в ООП-языках',
          ],
          correctIndex: 1,
          explanation:
            'Strategy: вместо гигантского if/else разные варианты поведения вынесены в отдельные объекты-стратегии. Основная функция не знает деталей реализации — она только выбирает и вызывает нужную стратегию. Пример: calcDeliveryPrice(order, "courier") → выбирает courierStrategy.calc(order). Преимущества: легко добавить новый вариант (новая стратегия без изменения основного кода), каждую стратегию можно тестировать отдельно, код читаемее. Когда применять: расчёт цен по типу клиента, разные способы оплаты/доставки, разные алгоритмы сортировки.',
        },
        // ✅ Theory #93 — точная формулировка
        {
          type: 'multiple-choice',
          question: 'Что такое Dependency Injection (внедрение зависимостей) как паттерн проектирования? Какую проблему он решает?',
          options: [
            'DI — это когда зависимости создаются внутри класса через new',
            'DI: компонент получает зависимости извне (через конструктор/параметры), а не создаёт их сам. Решает проблему жёсткой связанности: зависимость легко заменить, код проще тестировать (можно подменить зависимость на mock)',
            'DI — это инструмент только для Angular',
            'DI решает проблему circular imports',
          ],
          correctIndex: 1,
          explanation:
            'Проблема без DI: class UserService { constructor() { this.api = new ApiClient(); } } — UserService жёстко привязан к ApiClient: нельзя подменить для тестов, нельзя переиспользовать с другим API. DI-решение: class UserService { constructor(apiClient) { this.api = apiClient; } } — зависимость передаётся снаружи. Преимущества: тестируемость (передаём mock), гибкость (разные реализации), явные зависимости (видно что требует класс), слабая связанность. DI используется везде: React (передача через props/context), Angular (встроенный DI-контейнер).',
        },
        // ✅ Theory #83 (компонентный подход) — дополнительный вопрос
        {
          type: 'multiple-choice',
          question: 'Что такое композиция компонентов как архитектурный приём? В каких случаях она предпочтительнее наследования и почему?',
          options: [
            'Композиция — это то же самое что наследование, только другое слово',
            'Композиция: крупный компонент собирается из более мелких. Предпочтительнее наследования когда нужна гибкость (можно менять части независимо), переиспользование без жёсткой иерархии, и избежать хрупкого базового класса',
            'Наследование всегда лучше — оно мощнее',
            'Композиция применима только в React',
          ],
          correctIndex: 1,
          explanation:
            'Композиция: "has-a" — объект содержит другие объекты. Наследование: "is-a" — класс расширяет другой. Почему композиция предпочтительнее: (1) Гибкость: можно менять части независимо, не ломая иерархию. (2) Нет проблемы "хрупкого базового класса" — изменение родителя не ломает потомков. (3) Переиспользование без жёсткой иерархии: Header = Logo + NavMenu + SearchBar. (4) Легче тестировать части отдельно. В React: вместо class AdminButton extends Button — <Button onClick={...} variant="admin" />. Правило: предпочитай композицию наследованию (GoF).',
        },
        // ✅ Theory #91 / #92 / #100 — точная формулировка FSD
        {
          type: 'multiple-choice',
          question: 'Что такое FSD-архитектура? Какие основные слои существуют в Feature-Sliced Design и за что они отвечают?',
          options: [
            'FSD — это фреймворк для React, заменяющий Redux',
            'FSD (Feature-Sliced Design) — архитектурный подход с однонаправленными зависимостями. Слои: app (настройки), pages (страницы), widgets (блоки), features (функциональность), entities (доменные сущности), shared (переиспользуемые утилиты). Зависимости только вниз',
            'FSD — это просто название структуры папок с components и utils',
            'В FSD нет правил зависимостей между слоями',
          ],
          correctIndex: 1,
          explanation:
            'FSD (Feature-Sliced Design): app → pages → widgets → features → entities → shared. app: провайдеры, роутинг, конфиг. pages: конкретные экраны/роуты. widgets: крупные UI-блоки (шапка, боковое меню). features: пользовательские возможности (логин, добавление в корзину). entities: доменные сущности (User, Product, Order) с моделью и UI-примитивами. shared: переиспользуемые утилиты, UI-примитивы, без знания о бизнес-логике. Правила: pages знает features; features знает entities; entities знает только shared. Нельзя импортировать из верхнего слоя в нижний.',
        },
        // ✅ Theory #99 — точная формулировка
        {
          type: 'multiple-choice',
          question: 'Как избежать циклических зависимостей в FSD?',
          options: [
            'Циклические зависимости неизбежны и не являются проблемой',
            'FSD запрещает зависимости между сущностями одного слоя (entities не может импортировать из entities). Общую логику выносят в shared. Используют barrel-файлы (index.ts) как публичный API модуля',
            'Нужно использовать dynamic import для всех модулей',
            'Циклические зависимости решаются через setTimeout',
          ],
          correctIndex: 1,
          explanation:
            'Циклические зависимости в FSD предотвращаются правилами: (1) Запрет горизонтальных зависимостей: entities/user не может импортировать из entities/product. (2) Общая логика → shared: если нужна между слайсами одного слоя, выносим в shared. (3) Публичный API через index.ts: импортировать только из публичного API модуля, не из его внутренностей. (4) Запрет deep imports: нельзя import { X } from "@/entities/user/model/store" — только import { X } from "@/entities/user". (5) Линтер (eslint-plugin-boundaries) контролирует правила автоматически.',
        },
        // ✅ Theory #101 — точная формулировка
        {
          type: 'multiple-choice',
          question: 'Когда использование EventEmitter / Pub-Sub оправдано вместо props и Context, и какие у этого подхода риски?',
          options: [
            'EventEmitter всегда лучше props — он глобальный и удобный',
            'EventEmitter оправдан когда компоненты в разных частях дерева не связаны иерархически, или при интеграции с внешними системами. Риски: сложно отследить поток данных, утечки памяти (не отписались), трудно тестировать',
            'EventEmitter нельзя использовать во фронтенде',
            'EventEmitter — замена Redux, всегда предпочтительнее',
          ],
          correctIndex: 1,
          explanation:
            'EventEmitter/Pub-Sub оправдан: компоненты далеко в дереве без общего предка, интеграция с внешними библиотеками/SDK, кросс-модульная коммуникация без прямых зависимостей. Риски: (1) Неявный поток данных — сложно понять кто что слушает. (2) Утечки памяти — если не вызвать removeEventListener/off при размонтировании. (3) Трудно тестировать — нужно эмитировать события вручную. (4) Порядок подписок не гарантирован. (5) Нет единого источника правды. Предпочитай props→Context→store; EventEmitter — крайний случай.',
        },
      ],
    },
  ],
};
